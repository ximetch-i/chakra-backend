spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chakra_dev
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.chakra: DEBUG
